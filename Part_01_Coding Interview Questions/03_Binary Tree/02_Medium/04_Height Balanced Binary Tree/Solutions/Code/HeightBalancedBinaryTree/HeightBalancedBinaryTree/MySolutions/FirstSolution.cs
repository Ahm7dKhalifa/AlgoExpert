using System;
using System.Collections.Generic;
using System.Text;

namespace HeightBalancedBinaryTree.MySolutions
{
    public class FirstSolution
    {
        #region Algorithm Design 
        /*
          
          //base case 1
          if(currentNode == null)
             return 0
          
          //business logic
          var leftHeight = GetHeight(currentNode.Left);
          var rightHeight = GetHeight(currentNode.Right);
          result.isTreeHasHeightBalanced(leftHeight,rightHeight)
            
          

         */
        #endregion
    }
}
